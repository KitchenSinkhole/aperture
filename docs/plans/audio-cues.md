# Audio Cues

**Goal:** Opt-in sounds for the things a pilot cannot watch the map for: a tracked pilot entering or leaving their system (#215), a jump through a wormhole they chose to watch (#280), and a kill landing in an on-map system (#263), with a choice of built-in chimes, voice packs, and the user's own sound files.
**References:** CLAUDE.md (realtime, config, code style), `src/components/map/MapCanvas.md` (provider stack and toolbar), `src/components/map/MapPresenceContext.md` (`useTraversals`), `src/components/map/MapTravelContext.md` (`TravelBridge`, the bridge precedent), `src/components/map/MapUnderglowBridge.md`, `src/components/account/AccountSettingsDialog.md`, `src/app/(app)/actions/account.md`, `src/lib/session.md`, `src/db/schema/ap/user.md`, `src/lib/wormholePickerPrefs.md` (localStorage store precedent).

**Background:** All three issues are triaged from Discord and ask for the same thing with a different trigger. Today the codebase has exactly one audio call (the shift-click rally easter egg in `SystemOverlay.tsx`) and no sound engine, no volume control, no mute, and no handling of the browser autoplay policy. The event signals already exist on the client: the presence store derives a `Traversal` (character, from system, to system) for every tracked pilot move, the active-character context knows the viewer's current system, and the `systemNotification` envelope already drives the red kill underglow. Nothing here needs a server-side event or a new realtime task name.

**Key design decisions (agreed up front):**

- **One engine, many event sources.** `src/lib/sounds/engine.ts` owns Web Audio, volume, mute, autoplay unlock, cross-tab dedupe and coalescing. Each issue is a renderless bridge component that turns a realtime signal into `engine.play(event)`. Bridges follow the `TravelBridge` pattern: mounted by `MapCanvas` only when the account has that event enabled, so an off setting means the code path is absent, not gated at runtime.
- **The sound event vocabulary is fixed:** `pilotArrived`, `pilotLeft`, `watchedJump`, `killInSystem`. A future system-of-interest alert (#182) adds a fifth; nothing else does.
- **Account-level preferences, one jsonb column.** `ap_user.sound_prefs` typed `SoundPrefs` (`{ enabled, volume, events: Record<SoundEvent, { enabled, sound }> }`), NULL meaning every default. Follows the `map_layout` precedent (typed jsonb, Zod-validated at the Server Action boundary, threaded to `MapCanvas` as a prop, `revalidatePath('/', 'layout')` on save). Defaults: master off, every event off, volume 0.7, one built-in chime per event. Per-map profiles are deliberately out of scope.
- **Device-level mute plus unlock indicator on the map toolbar.** A speaker button beside `PilotRosterButton`. Mute is localStorage (`aperture:sounds:muted`), so it survives reload and applies to every tab on the device. The same button shows a locked state while the browser has not yet allowed audio (no user gesture since page load) and clicking it is itself the unlocking gesture.
- **Built-in chimes are synthesized with Web Audio, not shipped as files.** A soft rising two-tone for arrive, falling for leave, a short tick for a watched jump, a harsher tone for a kill. No assets, no fetch latency, volume via one master `GainNode`. The voice packs (Stage 8) and custom files (Stage 9) are file-backed and decoded into `AudioBuffer`s through the same engine.
- **Sound ids are strings with a namespace.** Built-in ids are a fixed union (`chime-up`, `chime-down`, `tick`, `alarm`, later `voice-<pack>-<event>`); custom ids are `custom:<uuid>`. `SoundPrefs` stores the id, so an account pref that names a custom sound missing on this device falls back to that event's default chime.
- **Custom sound files never touch the server.** They live in the browser's IndexedDB per device, validated by decoding them with the `AudioContext` on import, capped in size and duration. Self-hosted deployments have no object store, and the server has no business holding audio.
- **Cross-tab: one tab per map plays.** The SharedWorker gives one socket per origin but every subscribed tab receives every envelope, so without dedupe N tabs on the same map play N sounds. The engine elects a leader per map with the Web Locks API (`navigator.locks.request('aperture:sound-leader:<mapId>')`, held until the tab closes); non-leaders stay silent, and the next waiter takes over when the leader goes. Where Web Locks is unavailable the engine plays unconditionally.
- **Coalescing is per event, leading edge.** The first event of a kind plays; further events of the same kind inside `SOUND_COALESCE_MS` (2000, a module constant) are dropped. This satisfies #215's "suppress repeated chimes on rapid jumps" without a queue.
- **The viewer's own characters never trigger a sound.** Arrive, leave and watched-jump all skip traversals whose `characterId` is one of `viewerCharacterIds`, the same filter `TransitSignaturePrompt` applies.
- **Sounds play regardless of tab visibility.** Hearing it while tabbed out is the point.
- **Autoplay failures are silent.** A `play()` while the context is suspended is dropped, never toasted; the toolbar indicator is the only surface for "audio is locked".
- **No reconnect grace window is needed.** A socket reconnect re-seeds the presence store from a fresh snapshot, and `seed()` never emits traversals (`MapPresenceContext.md`), so a catch-up cannot produce a chime burst.
- **Watched wormholes are personal and device-local.** A watch is `(mapId, connectionId)` in localStorage (`aperture:map:<mapId>:watched-connections`), dropped when its connection is deleted, never when it merely leaves the rendered view (removing a system hides its connections but keeps the rows, and a restore brings the hole back under the same id). Connections hard-delete on collapse, so a watch has the lifetime of the hole; entries a device was away for are reclaimed by a TTL wider than any hole can live. A shared "everyone hears this hole" mode is a possible later variant and is not designed here.
- **Watched-jump direction is relative to the viewer.** When the viewer's active character sits at one end of the watched hole, the event carries `variant: 'inbound' | 'outbound'`; otherwise `variant: 'plain'`. Built-in chimes ignore the variant; the voice packs use it.

**Non-goals:** Browser `Notification` API, Discord or EVE-mail delivery (#182's other channels), per-event volume, server-stored audio, the rally easter egg (left as is), the public spectator layout.

---

## Stage 1 — Sound engine and built-in chimes
**Mode:** Execute
**Status:** done — a813ad0c
**Goal:** A browser-only engine that can synthesize the four built-in chimes and play a named sound event with volume, mute, autoplay unlock, per-map leader election and per-event coalescing, proven by unit tests against a fake audio backend.
**References:** `src/lib/wormholePickerPrefs.md` (subscribable localStorage store shape), `src/lib/realtime/useRealtime.md` (for the `useSyncExternalStore` idiom used client-side), `tests/unit/pip-window-prefs.test.ts` (localStorage test precedent).
**Touches:** new `src/lib/sounds/prefs.ts` (+ `.md`): `SoundEvent`, `SoundId`, `SoundPrefs`, `DEFAULT_SOUND_PREFS`, `soundPrefsSchema` (Zod), `resolveSoundPrefs(raw: unknown): SoundPrefs`; pure, no browser or server imports, safe for `session.ts` and Server Actions. New `src/lib/sounds/catalog.ts` (+ `.md`): the built-in catalog `{ id, label, event default, synth(ctx) => AudioBuffer }` for `chime-up`, `chime-down`, `tick`, `alarm`. New `src/lib/sounds/engine.ts` (+ `.md`): lazily created `AudioContext` + master `GainNode`; `unlock()` bound to the first `pointerdown` / `keydown` on the document; `play(event, { variant })` resolving the event's sound id through prefs, coalescing, mute and leadership; `preview(soundId)` bypassing master, mute and leadership; `setPrefs`, `setMapId`; a subscribable `{ unlocked, muted }` snapshot for the toolbar; mute read/written through localStorage. The audio backend is an injectable interface so tests run on a fake. New `src/lib/sounds/mutePrefs.ts` if the mute store is cleaner separate (implementer's call). `src/types/index.ts` (+ `.md`) re-exports `SoundEvent`, `SoundId`, `SoundPrefs`. Tests `tests/unit/sound-engine.test.ts`, `tests/unit/sound-prefs.test.ts`.
**Done when:** `pnpm test` covers: `resolveSoundPrefs` fills every default from `null`, a partial object, and garbage; an unknown or `custom:` id with no registered buffer resolves to the event's default chime; `play` is a no-op when the engine is locked, muted, master-disabled, event-disabled, or not the map leader; two `play` calls of one event inside the coalesce window produce one backend call and a different event inside the window still plays; leadership passes when the fake lock releases; `preview` plays while muted and master-disabled. `pnpm lint`, `pnpm typecheck`, `pnpm build` green. Nothing in the app mounts the engine yet.

## Stage 2 — Persist sound preferences
**Mode:** Execute
**Status:** done — b4905654
**Goal:** The account's `SoundPrefs` round-trip through `ap_user.sound_prefs` via a session getter and a Server Action.
**References:** `src/db/schema/ap/user.md`, `src/lib/session.md`, `src/app/(app)/actions/account.md` (`setMapLayoutAction` is the template), memory: `db:generate` works, `.rollback.sql` is hand-written.
**Touches:** `src/db/schema/ap/user.ts` (+ `.md`): `sound_prefs` nullable `jsonb` `.$type<SoundPrefs>()`; new migration via `pnpm db:generate` plus a hand-written `.rollback.sql`; `src/lib/session.ts` (+ `.md`): `getSoundPrefs(userId): Promise<SoundPrefs>` running the column through `resolveSoundPrefs`; `src/app/(app)/actions/account.ts` (+ `.md`): `setSoundPrefsAction(prefs: unknown)` validating with `soundPrefsSchema`, writing the column and `updated_at`, revalidating the `/` layout; `src/types/index.ts` if the inferred row type needs no change, confirm only.
**Done when:** Migration applies and rolls back cleanly against the dev DB; `setSoundPrefsAction` rejects a malformed blob with `{ ok: false }` and accepts a valid one; `getSoundPrefs` on a fresh account returns `DEFAULT_SOUND_PREFS`. CI green.

## Stage 3 — "Sounds" section in Account Settings
**Mode:** Execute
**Status:** done — f2d61244
**Goal:** A user can turn sounds on, set the volume, and pick and preview a built-in sound per event, and the choice follows their account.
**References:** `src/components/account/AccountSettingsDialog.md` (optimistic state + `startTransition` + rollback toast pattern), `src/components/chrome/CharacterPanel.md`, `src/app/(app)/layout.tsx`, `src/lib/sounds/catalog.md`, `src/lib/sounds/engine.md`.
**Touches:** `src/components/account/AccountSettingsDialog.tsx` (+ `.md`): new `soundPrefs: SoundPrefs` prop and a "Sounds" section under the signature indicators: master checkbox, volume slider (disabled when master off), one row per `SoundEvent` with an enabled checkbox and a sound select plus a preview button; every commit sends the full `SoundPrefs` through `setSoundPrefsAction` with rollback on failure. `engine.preview` already unlocks on its own when audio is locked, so the dialog needs no separate unlock gesture; it may still preview an event's chime as a checkbox is enabled. New `src/components/account/SoundPicker.tsx` (+ `.md`) if the select+preview row is cleaner extracted. `src/components/chrome/CharacterPanel.tsx` (+ `.md`) threads the prop; `src/app/(app)/layout.tsx` calls `getSoundPrefs`. Event labels live in the catalog, not the dialog.
**Done when:** Toggling and saving each control persists (verified by reload) and a failed action rolls back with a toast; preview plays with sounds otherwise off; CI green. The map does not react to the prefs yet.

## Stage 4 — Toolbar mute and unlock indicator
**Mode:** Execute
**Status:** done — 0719ec5f
**Goal:** The map toolbar shows whether sounds can play right now and offers a one-click device mute.
**References:** `src/components/map/MapCanvas.md` (toolbar cluster, `PilotRosterButton` placement, prop threading), `src/components/map/PilotRosterButton.md`, `src/lib/sounds/engine.md`, `src/app/(app)/map/[[...slug]]/page.tsx`.
**Touches:** new `src/components/map/SoundToolbarButton.tsx` (+ `.md`): ghost icon button subscribing to the engine snapshot (Stage 3's dialog is mounted on every authenticated page and already pushes the account prefs into the engine, so the engine — and its document gesture unlock — exists before a map opens; `MapCanvas` still owns `setMapId`); three states with tooltips: locked ("Click to enable sounds"), muted, live; click unlocks when locked, otherwise toggles mute; hidden entirely when the account's master switch is off. `src/components/map/MapCanvas.tsx` (+ `.md`): new `soundPrefs: SoundPrefs` prop, mounts the button in the toolbar and claims the map's leader lock with `engine.setMapId` in an effect. It must **not** call `engine.setPrefs`: the dialog already owns that write and holds the optimistic blob, and a second writer would let a canvas re-render stamp the server copy back over an unsaved change. `soundPrefs` is read-only here, deciding which sound surfaces mount. `src/app/(app)/map/[[...slug]]/page.tsx` adds `getSoundPrefs` to the parallel preload.
**Done when:** `pnpm test` covers the button's three states and what a click does in each: locked click unlocks and does not mute, unlocked click toggles mute, and a status change pushed from outside re-renders it. CI green. The locked→live transition, mute surviving a reload and the button's absence with the master switch off are in `## Manual verification` — jsdom has neither Web Audio nor an autoplay policy to unlock.

## Stage 5 — Pilot arrived and pilot left (#215)
**Mode:** Execute
**Status:** done — 030ed047
**Goal:** A tracked pilot moving into or out of the viewer's current system plays the arrive or leave sound.
**References:** `src/components/map/MapPresenceContext.md` (`useTraversals`, `Traversal`), `src/components/map/MapActiveCharContext.md` (`activeCharSystemId`), `src/components/map/MapTravelContext.md` (`TravelBridge` as the bridge template), `src/components/map/TransitSignaturePrompt.md` (own-character filter), `tests/unit/presence-store.test.ts`.
**Touches:** new `src/lib/sounds/presenceEvents.ts` (+ `.md`): pure `classifyTraversal(t, mySystemId, viewerCharacterIds): 'pilotArrived' | 'pilotLeft' | null`; new `src/components/map/PresenceSoundBridge.tsx` (+ `.md`): renderless, subscribes to `useTraversals`, reads `useMapActiveChar().activeCharSystemId`, calls `engine.play`; `src/components/map/MapCanvas.tsx` (+ `.md`): mounts the bridge inside `MapActiveCharProvider` only when `soundPrefs.enabled` and either event is enabled, passing `viewerCharacterIds`; tests `tests/unit/presence-sound-events.test.ts`.
**Done when:** Unit tests cover: move into my system → arrived; move out → left; move between two other systems → null; my own character → null; no active character located → null. CI green. Closes #215 once the manual check passes.

## Stage 6 — Watch a wormhole (#280)
**Mode:** Execute
**Status:** done — f07a0a8c, 67443fb3 (watch-set expiry)
**Goal:** A user can mark a wormhole as watched from its context menu, see which holes they watch, and hear a sound when a tracked pilot jumps one, with inbound and outbound variants when they sit at one end.
**References:** `src/components/map/MapContextMenu.md` (connection block, self-contained item precedent `SetDestinationItem`), `src/components/map/ConnectionEdge.md` (badge row), `src/components/map/MapTravelContext.md` (traversal → edge resolution), `src/lib/wormholePickerPrefs.md`, `tests/unit/set-destination-item.test.tsx`.
**Touches:** new `src/lib/connectionWatchPrefs.ts` (+ `.md`): per-map localStorage store with subscribe/notify, `readWatchedConnections`, `isConnectionWatched`, `toggleConnectionWatch`, `dropWatchedConnection(mapId, connectionId)` (called for a committed `connection.delete`, not a prune against the rendered set), `useIsConnectionWatched`; each entry is stamped with the instant the watch was set and expires after `apertureConfig.WATCHED_CONNECTION_TTL_MS` (4 days, added in `aperture.config.ts`), applied when a map's snapshot is built. `src/components/map/MapTravelContext.tsx` (+ `.md`): export the existing traversal-to-edges resolver as a pure helper so the new bridge reuses it rather than duplicating it. New `src/components/map/WatchConnectionItem.tsx` (+ `.md`): a `MenuCheckboxItem` "Watch this wormhole" in the `connection` block of `MapContextMenu` (+ `.md`), self-contained like `SetDestinationItem`, shown only for `wh`-scope connections. `src/components/map/ConnectionEdge.tsx` (+ `.md`): a small watched badge in the text-badge row driven by the store. New `src/components/map/WatchedConnectionSoundBridge.tsx` (+ `.md`): subscribes to `useTraversals`, resolves edges, skips own characters, computes the variant from the viewer's system resolved live from the presence store (not from `activeCharSystemId`; see the Stage 5 note), plays `watchedJump`. `src/components/map/MapCanvas.tsx` (+ `.md`): mounts the bridge when the event is enabled and drops a watch wherever it applies a committed `connection.delete` (realtime apply, single-delete `onCommitted`, `onBulkPaste`). Tests `tests/unit/connection-watch-prefs.test.ts` and a pure-function test for the variant computation.
**Done when:** Unit tests cover watch toggle persistence, the drop on delete, and expiry (an entry past the TTL is dropped and the key compacted, one inside it survives, re-watching restarts the clock); variant is inbound when the traversal ends in my system, outbound when it starts there, plain otherwise; own character skipped. The menu item and badge render only for wormhole connections. CI green. Closes #280 once the manual check passes.

## Stage 7 — Kill in system (#263)
**Mode:** Execute
**Status:** done — 7b5f7340
**Goal:** A zKB kill in any on-map system plays the kill sound alongside the existing red underglow.
**References:** `src/components/map/MapUnderglowBridge.md`, `src/lib/realtime/protocol.md` (`systemNotificationLoadSchema`, kind `killmail`).
**Touches:** new `src/components/map/KillSoundBridge.tsx` (+ `.md`): a `useRealtimeEvents` listener mirroring `MapUnderglowBridge`'s envelope guard and parse, playing `killInSystem` for `kind === 'killmail'` only (a `ping` is not a kill); `src/components/map/MapCanvas.tsx` (+ `.md`): mounts it when the event is enabled.
**Done when:** A `killmail` notification for the open map plays once (coalesced across a same-tick burst), a `ping` plays nothing, a foreign-map envelope plays nothing; CI green. Closes #263 once the manual check passes.

## Stage 8 — Voice packs
**Mode:** Barrier
**Status:** todo
**Goal:** Three recorded voice packs (Charmion: calm Aura-style; David: southern cowboy; Malyx: echoing demon), each covering every event with the watched-jump line split by direction variant, are selectable per event so a user can mix packs across events.
**References:** `src/lib/sounds/catalog.md`, `src/lib/sounds/engine.md`, `src/lib/sounds/prefs.md`, `src/components/account/SoundPicker.md`, `public/sounds/`.
**Touches:** `public/sounds/voice/<pack>/<event>.ogg` and `public/sounds/voice/<pack>/watchedJump-<variant>.ogg` (18 assets: `pilotArrived`, `pilotLeft`, `killInSystem`, and `watchedJump-plain` / `-inbound` / `-outbound` for each of `charmion`, `david`, `malyx`); `src/lib/sounds/prefs.ts` (+ `.md`): a `VOICE_PACKS` constant (`{ id, label }` per pack) and `voice-<pack>-<event>` ids joining `BUILT_IN_SOUND_IDS`, since `soundPrefsSchema` rejects any save naming an unknown id and `resolveSoundPrefs` rewrites one back to the event's chime; `src/lib/sounds/catalog.ts` (+ `.md`): file-backed catalog entries generated from `VOICE_PACKS × SOUND_EVENTS` as `{ id, label, pack, event, files: Record<variant, path> }` (non-watched events map every variant to the one file), lazily fetched and decoded on first use, prefetched when selected in prefs; `src/lib/sounds/engine.ts` (+ `.md`): `BuiltInSound.synth` is synchronous and `SoundBackend.has` is synchronous, so a file-backed entry needs both widened, plus a prefetch on `setPrefs`, since an unfetched voice file reports `has() === false` and silently falls back to the chime; `src/components/account/SoundPicker.tsx` (+ `.md`): one option group per pack, labelled with the pack name, listing only that event's entry (it builds its options from `BUILT_IN_SOUNDS`, so widening the catalog alone puts every pack's every event in every select, unlabelled).
**Done when:** Every voice sound previews from the picker and each event's select shows exactly one option per pack; a watched jump with `variant: 'inbound'` plays the chosen pack's inbound line and a `variant: 'plain'` jump plays its plain line; a missing file falls back to the event's chime silently; CI green.
Barrier because the audio files do not exist yet: they are generated outside the repo through a TTS service (never lifted from the game client), trimmed of leading silence, loudness-normalised across all 18 files, and converted to `.ogg`. The line list is settled (see Notes). Provide the files, then run the code side.

## Stage 9 — Custom sound files
**Mode:** Execute
**Status:** todo
**Goal:** A user can add their own short sound files on this device, pick them per event, and remove them.
**References:** `src/lib/sounds/engine.md`, `src/lib/sounds/catalog.md`, `src/components/account/AccountSettingsDialog.md`.
**Touches:** new `src/lib/sounds/customStore.ts` (+ `.md`): IndexedDB store of `{ id: 'custom:<uuid>', label, mime, blob, durationMs }` with `list`, `add(file)`, `remove(id)`, a subscribe/notify surface, and validation on add (decode through the `AudioContext`, reject over `CUSTOM_SOUND_MAX_BYTES` / `CUSTOM_SOUND_MAX_MS` module constants); `src/lib/sounds/engine.ts` (+ `.md`): register decoded custom buffers with `createWebAudioBackend` so its `has`/`play` answer for `custom:` ids — the engine core already falls back to the event's default chime for an id the backend lacks, so it needs no change; `src/components/account/AccountSettingsDialog.tsx` (+ `.md`): a "Your sounds" list with file input, preview and delete; `src/components/account/SoundPicker.tsx` (+ `.md`): real custom entries in each event's select, replacing the single muted "Custom sound" placeholder it shows today for an id this device does not know; tests `tests/unit/custom-sound-store.test.ts` with a fake IndexedDB or the decode step injected.
**Done when:** Add, preview, select, reload, delete all work; an oversize or undecodable file is rejected with a toast and nothing stored; a pref naming a deleted custom id plays the event's default chime; CI green.

## Manual verification
_(worked by the user once, after the run; the plan is not complete until it passes)_
- **Stage 3** — Open Account Settings, enable sounds, move the volume slider, pick a chime per event, preview each; reload and confirm every choice stuck. With the master switch off, a preview still plays and at the slider's volume.
- **Stage 3** — On a short viewport, open Account Settings and scroll to the Sounds and Delete sections: the "Account settings" title and the X stay fixed at the top while the body scrolls.
- **Stage 4** — Load a map in a fresh tab with sounds enabled: the toolbar speaker shows locked; click anywhere and it goes live; mute it, reload, still muted.
- **Stage 4** — With the account's master sound switch off, the map toolbar shows no speaker button at all.
- **Stage 5** — With a second account's tracked pilot jumping into and out of your system, hear the up chime on entry and the down chime on exit; jump one of your own alts in and hear nothing; have three pilots jump in within a second and hear one chime.
- **Stage 5** — Tab away from the map (another tab in front) and confirm the chime still plays.
- **Stage 5** — Open the same map in two tabs; one chime per event, not two. Close the tab that played and confirm the other takes over.
- **Stage 6** — Right-click a wormhole, watch it, see the badge; have a tracked pilot jump it from your side and hear the outbound variant, back and hear inbound; watch a hole you are not on and hear the plain cue; delete the hole and confirm the badge and the watch are gone.
- **Stage 6** — Watch a wormhole, then make the server delete it through a bulk path from this same tab: re-scan its source system and paste the sigs with orphan removal on (or *Delete subchain* / *Delete disconnected* covering it). The badge and the watch must be gone the moment the paste returns, with the map's `aperture:map:<id>:watched-connections` key no longer listing that id.
- **Stage 6** — Watch a wormhole, right-click the system at its far end and *Remove from map*, then bring that system back (re-paste the sig and accept the restore prompt): the hole returns still watched, badge and all. Then delete the hole itself and confirm the watch and badge are gone.
- **Stage 7** — With a kill feed active, a kill in an on-map system plays the kill sound together with the red underglow.
- **Stage 8** — Select a different pack for each event and repeat the Stage 5 and 6 checks by ear; the packs must not bleed into each other. Then put Malyx on the watched hole and confirm plain, inbound and outbound are three different lines.
- **Stage 9** — Add a custom file, pick it for arrive, hear it; open the same account on another device and confirm arrive falls back to the chime.

## Notes
_(appended by executing sessions; non-obvious findings only)_

- **Stage 1** — jsdom has neither Web Audio nor Web Locks, which is why both the playback backend and the leader election are injected rather than probed for. Later stages testing audio can reuse the two fakes in `tests/unit/sound-engine.test.ts`; the catalog's `synth` only needs `sampleRate` + `createBuffer`, so a six-line object stands in for `BaseAudioContext`.
- **Stage 1** — `SoundVariant` is re-exported from `src/types/index.ts` alongside the three types the stage listed; Stages 6 and 8 both need it and it belongs to the same vocabulary.
- **Stage 1** — the stage's `Done when` enumerated every engine gate but never asked for the design's "applies to every tab" mute, so a `storage`-event-less store passed it; cross-tab mute propagation is now covered in `tests/unit/sound-engine.test.ts`.
- **Stage 2** — the persistence round trip lives in `tests/integration/sound-prefs-persistence.test.ts`, which is `describe.skipIf(!RUN_DB_TESTS)`. A plain `pnpm test` skips it, so a green CI run is *not* evidence that Stage 2's `Done when` holds; it has to be `RUN_DB_TESTS=1 pnpm test` against a migrated dev DB.
- **Stage 2** — rolling a migration back on the dev DB needs the `drizzle.__drizzle_migrations` row deleted by hand before `pnpm db:migrate` will re-apply it; the `.rollback.sql` only undoes the DDL. Verified apply → roll back → re-apply for `0071` this way.
- **Stage 2** — `soundPrefsSchema` is strict about sound ids, so the id union in `prefs.ts` is now load-bearing at the persistence boundary: any stage adding a sound id must widen `BUILT_IN_SOUND_IDS` or saves naming it are rejected. Stage 8's `Touches` was amended accordingly.
- **Stage 3** — `AccountSettingsDialog` is rendered (closed) by `CharacterPanel` on every authenticated page, including `/admin`, so its `getSoundEngine()` effect constructs the engine app-wide and its document gesture unlock is in place before any map opens. Stage 4's locked indicator therefore only shows for a tab whose very first page load is the map with no click yet.
- **Stage 3** — `getSoundPrefs` had to be threaded through the admin layout as well as `(app)/layout.tsx`: both render `CharacterPanel`, and the stage's `Touches` named only the app one.
- **Stage 3** — the dialog had no scroll container, and the Sounds section is what pushed it past the viewport; `DialogContent` is capped and scrolls for this dialog only, not in the `Dialog` primitive.
- **Stage 3** — rollback-on-failure is wired the same way as the signature-indicator commit but is not covered by a test: exercising it needs `setSoundPrefsAction` to fail, and rendering the dialog in jsdom means stubbing the base-ui `Dialog` and `Select` portals. Left unautomated rather than half-mocked.
- **Stage 3** — the stage's `Done when` verified persistence only by a manual reload, which cannot see a control that saves on one input path but not another, nor two commits overlapping; `tests/unit/account-settings-sounds.test.tsx` now covers the volume slider's commit triggers and the in-flight lock, and any later stage adding a control to this dialog should extend it.
- **Stage 4** — `AccountSettingsDialog` is the engine's sole `setPrefs` writer. It is mounted app-wide and holds the optimistic blob, so `MapCanvas` takes `soundPrefs` read-only (which surfaces to mount) and touches only `setMapId`. Stages 5–7 should keep that split: gate a bridge on the prop, never push the prop into the engine.
- **Stage 4** — the stage's `Done when` asked for "locked click unlocks and does not mute" without requiring the engine double to unlock mid-gesture the way the real one does, so a static double passed a button that muted on the unlocking click; `tests/unit/sound-toolbar-button.test.tsx` now drives a whole press (pointerdown, document capture unlock, click) and any later stage testing a gesture against the engine should do the same.
- **Stage 4** — `pnpm lint` is ESLint only; prettier is not a CI gate and `MapCanvas.tsx` is not prettier-clean in the repo. Running `prettier --write` on it reformats ~480 lines of unrelated code, so edit it by hand.

- **Stage 5** — `useTraversals` fires synchronously from inside `PresenceStore.apply()`, so a whole same-frame burst of `characterUpdate` envelopes is classified before React re-renders. Every render-scoped copy of `useMapActiveChar().activeCharSystemId` is a commit behind at that moment, and a ref refreshed by an effect does not help: `useTraversals` already parks the callback in its own ref on the same passive-effect flush, so both carry the same vintage. Two pilots jumping A→B in one frame would classify the second against A. `PresenceSoundBridge` therefore resolves the viewer's system live from the store — `usePresenceStore().getSystemForCharacter(activeCharId)`, the way `SignaturePasteHotkey` reads location at paste time — and keeps only the character id from the context. Stage 6's watched-jump bridge derives its variant from the same value and must resolve it the same way.
- **Stage 5** — the stage's `Done when` asked only for pure-function cases against `classifyTraversal`, which cannot see how the bridge sources "my system"; `tests/unit/presence-sound-bridge.test.tsx` now drives real `characterUpdate` frames through `RealtimeProvider` → `MapPresenceProvider` → the bridge, including two moves in one frame, and any later stage adding a traversal-driven bridge should test it at that level rather than only its classifier.
- **Stage 5** — no cue at all fires for a viewer with no located character, since "my system" is the only reference point the classification has. That is the whole of #215's model and not a gap, but it means the Stage 5 manual checks must be run with one of the viewer's own pilots logged in and located on the map.
- **Stage 5** — `MapActiveCharProvider` already wraps the entire canvas subtree, so Stage 6's bridge can sit anywhere in that stack and still read both `useTraversals` and `activeCharSystemId`; no provider reordering is needed for it.

- **Stage 6** — `MapConnectionEdge` carries no map id (only the canvas-built `ConnectionEdgeData` does), so `MapContextMenu` gained a `mapId` prop to key the watch store; the stage's `Touches` hadn't anticipated a prop change there.
- **Stage 6** — the variant helper `watchedJumpVariant` landed in `src/lib/sounds/presenceEvents.ts` beside `classifyTraversal` rather than in the bridge component, so the pure-function test imports no React. Any later stage adding a traversal-derived decision should keep it in that module.
- **Stage 6** — a watched hole reclassified away from `wh` through the same menu's `Type ▸` submenu would keep chiming with no item left to unwatch it, so `WatchConnectionItem` gates itself on `scope === 'wh' || watched` rather than the caller gating on scope alone.
- **Stage 6** — `connectionWatchPrefs` caches one snapshot per map id for the module's lifetime, so a test that seeds localStorage directly must use a map id nothing has read yet; `localStorage.clear()` alone does not invalidate the cache. Both Stage 6 test files take a fresh map id per test.
- **Stage 6** — `ConnectionEdge.tsx` is not prettier-clean in the repo either (Stage 4 found the same of `MapCanvas.tsx`), so it was hand-indented rather than run through `prettier --write`.
- **Stage 6** — the stage's `Done when` asked for a prune without saying against what, and `viewData.connections` is a *view*, not the live set: removing a system hides its connections while the rows (and the holes) survive, so a prune keyed to it destroyed watches for holes that were never deleted. A watch is ended by a committed `connection.delete`, which is the reading of the header's "pruned … on every connection delete" that holds its "a watch has the lifetime of the hole"; any later stage tempted to garbage-collect device-local map state must key it to a delete event rather than to what the canvas currently renders.
- **Stage 6** — keying the drop to `connection.delete` means finding every place the canvas applies one: the realtime listener is short-circuited for this tab by the `appliedEventIds` dedupe, so `onBulkPaste` (signature paste with orphan removal, `deleteSubchain`, `deleteDisconnected`) and the single-delete `onCommitted` hook each need their own drop. The stage's `Done when` tested the store in isolation and could not see a missed call site; any later stage reacting to a specific event kind in `MapCanvas` has the same three paths to cover.
- **Stage 6** — a delete this device never observed (tab closed, socket outage, or a server-side expiry job reaping the hole while nobody is logged in) is reconciled by a per-entry TTL rather than by server truth: a watch is stamped when it is set and dropped once it is older than `WATCHED_CONNECTION_TTL_MS`. The margin is what makes the guess safe — a hole's remaining life when a watch is placed on it can never exceed one full wormhole lifetime, so a 4-day entry cannot belong to a live hole. The alternative, pruning on load against a full server-side connection list, is the only reconciliation that reads real state, but it needs the snapshot fetch to return ids for connections belonging to invisible systems, which is a payload change outside this stage; it becomes the natural fix if the watch set ever moves server-side (per-account rather than per-device).
- **Stage 6** — expiry is applied when a map's snapshot is built, never on a timer, so a watch cannot vanish mid-session under a user looking at its badge; a long-lived tab keeps a stale watch until something invalidates the cache. A read that drops entries rewrites the compacted key without notifying, since the snapshot it returns already excludes them. The stored blob is a `{id: epochMs}` object, so the store treats an array or a non-numeric value as nothing watched.

- **Stage 7** — the kill bridge needs no `systems` prop. `MapUnderglowBridge` carries one to turn an EVE solar-system id into an `ap_map_system.id` for the glow store, not to decide membership: `zkbFeed` fans a `systemNotification` only to the maps that hold the system, so an envelope reaching this tab is a kill on a system the map holds, give or take the feed's index-refresh window: a system removed from the map can still fan a kill until the index is rebuilt, which the underglow drops for want of a node but the sound does not.
- **Stage 7** — the "coalesced across a same-tick burst" clause is the engine's behaviour, which the Stage 5 pattern of mocking `getSoundEngine` with a bare `vi.fn()` spy cannot see. `tests/unit/kill-sound-bridge.test.tsx` instead puts the *real* engine behind the mocked `getSoundEngine` (`vi.mock` with `importOriginal`, then `createSoundEngine` on a fake backend with an injected clock and `election: null`), so a burst of envelopes is asserted at the backend. Any later stage whose `Done when` names an engine gate should drive it the same way rather than counting bridge calls.
- **Stage 8 (prep)** — Voice lines, one file each, kept under two seconds because coalescing is leading-edge over a 2 s window and a longer line overlaps the next cue. Charmion: "Friendly pilot arriving." / "Friendly pilot departing." / "Activity on a watched wormhole." / "Wormhole activity. Pilot inbound." / "Wormhole activity. Pilot outbound." / "Kill reported in system." David: "Well, howdy. Y'all got company." / "There they go, y'all. Ride safe." / "Somebody's usin' that hole y'all been watchin'." / "Heads up, y'all. Rider comin' through the hole." / "One of ours just rode on out, y'all." / "Well, shoot. Somebody just got themselves killed." Malyx: the user picks one register (whisper / grand / hungry) and keeps it across all six lines so the pack sounds like one demon.
